请基于当前最新代码，继续修正 `apps/desktop`。当前版本已经完成了一部分正确方向的重构：已经有左侧导航、顶栏、主内容区、按页面切换、前端模块拆分，且审核/管理菜单开始按角色控制。`index.html` 已经引入 `nav-panel`、`topbar`、`page-outlet`，`app.js` 只保留了启动入口，`boot.js` 已经通过 `setupShellPages()` 和 router 做页面切换。:contentReference[oaicite:0]{index=0} :contentReference[oaicite:1]{index=1} :contentReference[oaicite:2]{index=2}

但当前实现还没有完全符合要求，请按下面问题逐项修正，不要另起炉灶，也不要偏离 `apps/desktop` 当前维护路径。

# 一、当前版本已存在的问题

## 1. 右上角登录入口还不是弹窗/抽屉式登录
当前 `topbar` 右上角虽然已经有“登录/用户”入口，但点击后实际逻辑是 `focusLoginEntry()`，会跳回首页并聚焦首页里的登录表单，而不是弹出登录框、抽屉或对话框。:contentReference[oaicite:3]{index=3} :contentReference[oaicite:4]{index=4}

这不符合要求。需要改成：
- 右上角“登录”按钮打开登录弹窗/抽屉/对话框
- 登录成功后自动关闭弹窗并刷新当前界面
- 登录失败时在弹窗内提示错误
- 已登录时右上角显示用户菜单，可退出登录

## 2. 未登录点击受保护功能，没有弹登录提示
当前未登录时：
- 发布表单只是被禁用
- 提交发布时只是显示一条状态文字
- 路由层对部分页面做了回退，但没有统一的“登录提示弹窗”交互

这不符合要求。需要改成：
- 未登录点击发布 Skill、我的 Skill、安装/启用等需鉴权功能时，弹出统一登录提示
- 文案类似：“该功能需要登录后使用”
- 提供“立即登录 / 取消”
- 不要只是禁用或静默跳回首页

## 3. `我的 Skill` 仍然对未登录用户可见
当前 `page-registry.js` 里 `my-skill` 仍然 `visibility: () => true`，虽然有 `requiresAuth: true`，但没有真正形成受保护入口体验。:contentReference[oaicite:5]{index=5}

请改成未登录时仍显示“我的 Skill”菜单，但点击后弹登录提示，不直接进入内容

## 4. 首页仍不是摘要工作台
当前首页仍然主要由 `hero + releaseBoundary + loginPanelSection` 组成。:contentReference[oaicite:6]{index=6}

这不符合要求。首页需要改成真正的摘要工作台，至少包含：
- 当前连接状态摘要
- 最近通知摘要
- 推荐或最近访问的 Skill
- 本地环境摘要（工具/项目状态摘要）
- 管理员可见的待审核摘要
- 已登录普通用户可见的“我的 Skill”摘要

不要再把首页当成登录页或品牌展示页。

## 5. 管理页仍不完整
当前管理区仍然只有 `Skill Management` 单一面板，`boot.js` 里也只看到 `renderManagementSkills()`，没有部门管理、用户管理页面骨架。:contentReference[oaicite:7]{index=7} :contentReference[oaicite:8]{index=8}

这不符合要求。请至少补齐管理页的二级结构：
- 部门管理
- 用户管理
- Skill 管理

即使当前后端还没有全部接口，也必须先把前端骨架、标签、空态、权限边界做出来。

## 6. 旧长页面源码仍然残留在 DOM 中
当前 `index.html` 里还保留了 `legacy-shell-source`，其中塞着旧版长页面的大量内容，只是 hidden。:contentReference[oaicite:9]{index=9}

这说明重构还没收干净。请：
- 删除不再需要的旧长页面遗留结构
- 保留必要功能节点即可
- 不要让新架构建立在“隐藏旧页面再搬运”的临时状态上

## 7. 错误处理仍不够桌面化
当前 `json()` 仍然直接 `response.json()`，如果后端返回 HTML 或其他非 JSON 内容，仍会出现 JSON 解析类错误。:contentReference[oaicite:10]{index=10}

请增强请求封装，至少明确区分并统一提示：
- 服务不可达
- 返回 HTML / 非 JSON
- 未登录
- 权限不足
- 后端业务错误
- 未知异常

要求：
- 顶栏或全局状态区显示主要连接状态
- 页面局部显示友好错误文案
- 不要在多个页面重复铺满同一种原始异常文案

## 8. 右上角用户菜单还不完整
当前右上角更多只是按钮，没有真正的用户菜单与退出登录交互。:contentReference[oaicite:11]{index=11}

请补齐：
- 当前用户名
- 当前角色
- 退出登录
- 已登录时再次点击可展开用户菜单
- 退出后立即更新导航和当前页面状态

# 二、本轮必须完成的修正目标

## A. 登录交互重做
实现真正的登录弹窗/抽屉/对话框：
- 右上角按钮触发
- 支持输入用户名、密码
- 登录成功关闭弹窗并刷新数据
- 登录失败在弹窗中提示
- 不再依赖首页登录面板作为主要登录入口

## B. 统一“需要登录”拦截
建立统一机制：
- 受保护按钮点击时，若未登录，则弹“请先登录”
- 受保护页面路由进入时，若未登录，则弹登录提示并保持安全页面
- 所有鉴权拦截走一套机制，不要每个页面各写一套

## C. 首页改成摘要工作台
首页必须改成摘要型，不再是 hero/品牌首屏。

## D. 管理页补齐结构
做成至少带二级标签的页面：
- 部门管理
- 用户管理
- Skill 管理

即使部门管理、用户管理当前先用空态或占位组件，也要把产品结构搭出来。

## E. 清理旧实现残留
删除隐藏的旧长页面遗留源码，不要继续保留 `legacy-shell-source` 这种中间过渡结构。

## F. 强化错误处理
把接口请求层抽出来，做统一异常分类与用户提示。

# 三、实施要求

1. 在现有结构上继续修正，不要推翻重来
2. 不要把工作迁移到其他已废弃或非当前维护路径
3. 不要再回到“大首页堆卡片”方案
4. 当前优先级高于视觉微调
5. 用户体验优先于样式炫技
6. 可以新增：
   - auth modal / drawer 组件
   - user menu 组件
   - request client / api helper
   - page tabs 组件
   - protected action helper
7. 若管理页后端接口不足，可以先做空态和占位，但必须把信息架构补齐

# 四、交付验收标准

本轮完成后，至少满足：

- 右上角点击“登录”会弹出登录对话框，而不是跳首页
- 未登录点击发布、我的 Skill、安装等受保护功能，会弹统一登录提示
- 登录成功后，导航和页面即时刷新
- 退出登录后，管理员菜单立即消失
- 首页是摘要工作台，不再是登录/品牌首屏
- 管理页已有部门管理、用户管理、Skill 管理三个子结构
- 隐藏的旧长页面遗留源码已清理
- 请求层已能区分 HTML 返回、未登录、权限不足、服务不可达等错误
- 管理员才看到审核与管理
- 普通用户与未登录用户都看不到部门管理等管理员功能

请直接继续修改代码，并在完成后说明：
- 本轮新增了哪些模块
- 删除了哪些旧结构
- 登录拦截机制如何实现
- 哪些页面已完全符合要求
- 哪些仍是占位但结构已补齐
