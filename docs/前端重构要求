请基于当前仓库 `HaibinZhang1/Enterprise-Agent-Hub`，重构 `apps/desktop`，目标不是“继续美化当前长页面”，而是把它改造成一个真正可用的 Windows-first 桌面客户端。

# 一、先理解当前状况

你必须先阅读并基于这些文件实施，不要脱离现有仓库瞎重写：

- `README.md`
- `docs/RequirementDocument/index.md`
- `docs/RequirementDocument/06_page_architecture.md`
- `docs/RequirementDocument/11_page_manage.md`
- `docs/RequirementDocument/13_page_notify_settings.md`
- `docs/DetailedDesign/desktop/README.md`
- `apps/desktop/package.json`
- `apps/desktop/src/server.js`
- `apps/desktop/ui/index.html`
- `apps/desktop/ui/app.js`
- `apps/desktop/ui/styles.css`
- `apps/desktop/src-tauri/src/main.rs`

当前项目文档已经明确：

- 当前维护的产品入口是 `apps/desktop`，`apps/web` 只是历史/非产品路径，不要把当前需求转移到 `apps/web`
- 桌面端应是 Windows-first 内网桌面客户端，不是网页原型
- 桌面端页面架构应是“左侧导航 + 顶栏 + 主内容区”的一级页面结构
- 一级页面应包括：首页、市场、我的 Skill、审核、管理、工具、项目、通知、设置
- 通知页、设置页都应是独立页面，不是首页的一块卡片
- 桌面端应以服务端为权威，以本地 SQLite 为执行缓存，突出本地执行、缓存、同步、工具与项目管理职责

但当前 `apps/desktop` 的实现存在明显偏差：

- 当前主要还是本地 Node server 提供静态页面
- 前端主界面是一个长滚动页面
- 登录、工具、项目、设置、我的 Skill、市场、通知、事件流、发布、审核、管理都堆在同一个页面里
- `app.js` 是单页总控脚本，而不是按页面/模块拆分
- 当前样式明显更像网页着陆页 / dashboard，而不是桌面软件工作台
- 当前 Tauri 层只是最基础壳，没有体现成熟桌面客户端的产品结构

目前实际效果是：界面像“网页卡片堆叠的大首页”，而不是“按功能分标签 / 分页面的桌面软件”。这与原始需求差距很大。

# 二、总体目标

把 `apps/desktop` 改造成“桌面壳 + 导航 + 分页面”的结构，至少达到以下产品形态：

- 左侧固定导航栏
- 顶部状态栏
- 中央主工作区
- 一次只展示一个一级页面
- 支持登录态与角色差异化菜单显示
- 保留当前已有业务能力，但重新安放到正确页面
- 不再保留“所有功能堆在一个首页”的结构

这不是一次单纯的视觉翻新，而是一次桌面客户端 UI 架构重构。

# 三、顶层布局要求

不要再保留当前“一屏往下滚的卡片堆叠首页”作为主结构。

目标布局必须是：

- 左侧固定导航栏
- 顶部工具栏 / 状态栏
- 中央主内容区
- 左上角为登录 / 用户入口
- 一次只展示一个一级页面

导航至少包括：

- 首页
- 市场
- 我的 Skill
- 工具
- 项目
- 通知
- 设置

管理员额外显示：

- 审核
- 管理

要求与需求文档中的页面架构保持一致，不要再做成所有内容同时出现在一个 HTML 页面里。

# 四、登录与权限交互要求

## 1）登录入口位置

桌面端左上角必须有明确的“登录 / 用户”入口：

- 未登录时显示“登录”按钮
- 已登录时显示当前用户名、角色信息、退出登录入口
- 顶栏同时承载连接状态、通知入口、用户入口
- 不要把登录表单长期铺在首页主内容区

推荐使用：
- 登录弹窗
- 登录抽屉
- 独立登录对话框

不要继续使用首页大块登录表单作为主要布局。

## 2）未登录时的行为

系统允许未登录进入桌面端并浏览不依赖后端鉴权的基础界面，但凡涉及后端鉴权、服务端会话、角色校验的操作，未登录点击时必须弹出统一的登录提示。

未登录时点击以下功能，应弹出登录提示框或登录抽屉，而不是直接报错：

- 发布 Skill
- 提交审核
- 审核相关操作
- 管理相关操作
- 需要拉取个人数据的“我的 Skill”
- 需要服务端授权的安装、启用、提交、审批、管理动作

提示文案方向：

- “该功能需要登录后使用”
- “请先登录企业内网账号”
- 提供“立即登录 / 取消”操作

## 3）游客态与登录态边界

### 未登录时
- 可以看到桌面壳、导航框架、首页摘要、部分公开市场浏览骨架
- 不应展示发布表单、审核队列、部门管理、用户管理、Skill 管理等受保护后台工作台
- 不应在主区域直接渲染需要会话的复杂表单
- 需要鉴权的数据区域应显示“请登录后查看”空态，而不是接口报错堆叠

### 普通用户登录后
- 显示：首页、市场、我的 Skill、工具、项目、通知、设置
- 不显示：审核、管理
- “我的 Skill”可见
- 发布入口可见，但提交类动作必须依赖有效会话

### 管理员登录后
- 在普通用户菜单基础上，额外显示：审核、管理
- “管理”下至少包括：部门管理、用户管理、Skill 管理
- 部门管理只有管理员登录后才展示，普通用户与未登录用户都不能看到入口

## 4）菜单显示规则

请严格按登录状态和角色动态渲染导航，不要只是在页面里隐藏几个卡片：

- 未登录：不显示审核、管理；发布等需要鉴权的入口若保留，点击时弹登录提示
- 普通用户登录后：仍不显示审核、管理
- 管理员登录后：才显示审核、管理
- 部门管理、用户管理、Skill 管理只有管理员登录后才展示

## 5）登录态变化要求

- 登录成功后立即刷新导航、权限可见项、当前页面数据
- 登录失败时在弹窗内给出明确错误提示
- 退出登录后，立即收起管理员菜单与受保护页面内容，并回到安全状态

# 五、页面必须按功能拆开

请按真正桌面客户端的信息架构重构，而不是只改样式。

## 1）首页
首页只做摘要页，不承载全部功能面板。

应展示：
- 当前连接状态
- 待办摘要
- 最近通知
- 推荐 / 最近使用 Skill
- 最近项目或本地环境状态摘要

不要把完整的 Tools / Projects / Settings / Publish / Review / Management 全堆在首页。

## 2）市场
做成独立页面，支持：
- 搜索
- 列表浏览
- 详情区或详情抽屉
- 安装 / 启用入口

未登录时可显示公开浏览骨架或受限空态；需要鉴权的安装动作点击时弹登录提示。

## 3）我的 Skill
做成独立页面，并带二级标签：

- 已安装
- 我发布的
- 发布 Skill

未登录点击该页面或进入需要个人数据的子页时，弹登录提示或显示“登录后查看”。

## 4）审核
管理员独立页面，并带二级标签：

- 待审核
- 审核中
- 已审核

只有管理员登录后才显示。

## 5）管理
管理员独立页面，并带二级标签：

- 部门管理
- 用户管理
- Skill 管理

即便后端当前还未完全完善，也要先把桌面端页面骨架和权限边界做对。

## 6）工具
做成独立页面，不再只是首页卡片。

建议：
- 左侧工具列表
- 右侧详情区
- 支持扫描、健康状态、绑定关系、修复预览

## 7）项目
做成独立页面，不再只是首页卡片。

建议：
- 左侧项目列表
- 当前项目详情
- 切换项目
- 校验 / 修复
- 技能绑定摘要

## 8）通知
做成独立页面。

支持：
- 列表展示
- 已读 / 未读状态
- 跳转关联对象

## 9）设置
做成独立页面。

至少区分：
- 连接设置
- 桌面偏好
- 更新通道
- 关于 / 版本信息

# 六、不要只做视觉翻新，必须做结构重构

这不是一次 CSS 改版，而是一次 UI 架构重构。

你需要：

- 重构 `apps/desktop/ui/index.html`
- 重构 `apps/desktop/ui/app.js`
- 必要时新增页面级模块文件、状态管理文件、组件文件
- 重构 `apps/desktop/ui/styles.css`
- 保持现有 `src/server.js` 的 API 能力可继续工作，但前端组织方式必须改掉

如果你认为纯静态 HTML + 单个 `app.js` 已经不适合当前复杂度，可以在 `apps/desktop` 内做合理的前端模块化改造，但不要把当前桌面产品工作迁移到 `apps/web`。

# 七、保留已有业务能力，但重新安放位置

已有能力不要丢，但要放到正确页面：

- 登录与会话
- 市场
- 我的 Skill
- 通知
- SSE 事件
- 发布工作台
- 审核队列
- 管理视图
- 工具扫描 / 修复
- 项目注册 / 切换 / 修复
- 设置保存

要求：

- 首页只显示摘要
- 详细操作进入对应一级页面
- 预览确认类动作用抽屉、模态框或右侧详情区承载
- SSE / 连接状态放全局区域，不要单独占一个大面板
- 发布、审核、管理不再和普通用户主流程混在首页

# 八、连接异常处理必须桌面化

当前界面大量出现：

`Unexpected token '<', "<!doctype "... is not valid JSON`

这说明当前 API base URL 配置错误时，前端会把 HTML 当 JSON 解析，随后在多个模块里一起报错。

你必须改进这一点：

- 连接状态应在顶栏或连接状态区统一显示
- API base URL 错误时，给出清晰、单点、可操作的错误提示
- 不要让整个界面到处铺满同一种 JSON 解析异常
- 至少区分：
  - 服务不可达
  - 返回 HTML 而非 JSON
  - 未登录
  - 权限不足
  - 接口异常

# 九、视觉方向

目标是“真正可用的企业桌面客户端”，不是宣传页。

视觉要求：

- 可以保留一定精致感，但不要再做夸张的 hero landing page
- 以稳定、克制、可长时间使用为主
- 更像桌面应用工作台，而不是官网首页
- 优先保证导航清晰、主内容聚焦、信息密度合理、滚动范围可控
- 使用Apple设计风格

# 十、角色与导航必须一致

请按需求文档处理角色菜单差异：

- 普通用户不显示“审核”“管理”
- 管理员显示对应菜单
- 通知角标、审核角标可预留或实现
- 设置放在导航底部或与主导航区分
- 部门管理、用户管理、Skill 管理仅管理员可见

# 十一、实施要求

1. 实施时优先保证 `apps/desktop` 可运行  
2. 不要把当前问题理解成“页面不好看”，而要理解成“产品结构错了”  
3. 不要继续扩张首页卡片数量  
4. 不要新增当前范围外的大功能；重点是把现有能力放到正确的桌面结构里  
5. 如需新增前端模块文件、页面文件、样式分层文件，可以直接做  
86. 实施后请输出：
   - 重构后的页面 / 模块结构
   - 导航与角色显示规则
   - 首页与各一级页面分别承载什么
   - 改了哪些文件
   - 当前还未完成但已预留的点
   - 同步修改对应的设计文档，README.md文档

# 十二、交付验收标准

以下条件同时满足才算完成：

- 打开桌面端后，不再是一个长滚动首页堆满所有卡片
- 存在清晰的左侧导航、顶栏、主内容区
- 左上角存在明确登录入口
- 能在一级页面之间切换
- 首页只是摘要，不是全集成功能页
- 市场 / 我的 Skill / 工具 / 项目 / 通知 / 设置是独立页面
- 审核 / 管理对管理员独立显示
- 普通用户登录后仍看不到管理员菜单
- 未登录时点击发布等受保护功能，会弹出登录提示，而不是直接请求失败
- 审核、部门管理、用户管理、Skill 管理仅在管理员登录后展示
- 发布、审核、管理不再和普通用户主流程混在首页
- 登录态变化后，导航和页面内容能即时切换
- 连接错误不再以满屏 JSON 异常卡片形式出现
- 仍然基于 `apps/desktop` 作为当前维护产品路径
- 改动是“桌面客户端结构重构”，不是“网页卡片重新排版”