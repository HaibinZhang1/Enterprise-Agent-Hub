{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Default desktop capability for the main Enterprise Agent Hub window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
